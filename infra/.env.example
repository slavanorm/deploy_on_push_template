DOMAIN=example.com
EMAIL=admin@example.com
REPO_PATH=/home/v0/repo
PERSIST_PATH=/home/v0/persist

# GitHub Secrets (add these in repo Settings → Secrets → Actions):
# HOST=<VPS IP or domain>
# USERNAME=<SSH user>
# SSH_KEY=<private key content>
# REPO_PATH=<path to repo on VPS, e.g. /home/v0/repo>
